import { Runner } from '../../@types';
export declare const resolveContainerId: ({ runner, runner: { logger, dockerEventStream$ } }: {
    runner: Runner;
}) => Promise<import("../bootstrap/createDockerEventEmitter").DockerEventType>;
