import { Runner } from '../../@types';
export declare const runReadinessCheck: ({ runner, runner: { containerId, dockerComposeFileService, readinessCheck, logger, dockerEventStream$ }, }: {
    runner: Runner;
}) => Promise<any>;
