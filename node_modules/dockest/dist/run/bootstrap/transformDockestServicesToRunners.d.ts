import { DockerEventEmitter } from './createDockerEventEmitter';
import { DockerComposeFile, RunnersObj, DockestService } from '../../@types';
export declare const transformDockestServicesToRunners: ({ dockerComposeFile, dockestServices, runMode, dockerEventEmitter, }: {
    dockerComposeFile: DockerComposeFile;
    dockestServices: DockestService[];
    runMode: import("../../@types").TestRunModeType;
    dockerEventEmitter: DockerEventEmitter;
}) => RunnersObj;
