import { DefaultReadinessChecks, Runner } from '../@types';
export declare const createDefaultReadinessChecks: ({ runner, runner: { containerId, dockerComposeFileService: { ports }, }, }: {
    runner: Runner;
}) => DefaultReadinessChecks;
